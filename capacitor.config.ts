import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitor-share-sheet-extensions',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
